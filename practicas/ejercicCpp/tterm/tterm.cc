#include <iostream.h>

int term;	// term used in two expressions
main()
{

    term = 3 * 5;
    cout << "Twice " << term << " is " << 2*term << "\n";
    cout << "Three times " << term << " is " << 3*term << "\n";
    return (0);
}
