// warning, spacing is VERY important 

#include <iostream.h>

#define MAX=10

main()
{
    int  counter;

    for (counter =MAX; counter > 0; 
      --counter)
        cout << "Hi there\n";

    return (0);
}
